#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define vll vector <long long>
 
 vll ar[1000001];
ll vis[1000001];
void dfs(ll node){
	vis[node]=1;
  cout<<node->;
	for(ll i=0;i<ar[node].size();i++){
		ll child=ar[node][i];
		if(vis[child]==0){
			dfs(child);
		}
	}
}
int main(){
  fast;
  ll n,m,a,b;
  cin>>n>>m;
  for(ll i=1;i<=m;i++){
	  cin>>a>>b;
     ar[a].pb(b);
	 ar[b].pb(a);
  }
dfs(1);

}













  
